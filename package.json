{
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/artevelde-uas/semantic-ui-react-spinbutton.git"
  },
  "packageManager": "yarn@3.2.0",
  "workspaces": [
    "packages/demo",
    "packages/docs",
    "packages/lib"
  ],
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "start": "run-p watch start:demo",
    "start:demo": "yarn workspace demo start",
    "build": "yarn workspace semantic-ui-react-spinbutton build",
    "watch": "yarn workspace semantic-ui-react-spinbutton watch",
    "test": "yarn workspace demo test"
  }
}
